name: Test Cron Every 5 Minutes

on:
  # schedule:
  #   - cron: "*/5 * * * *"
  workflow_dispatch: {}

jobs:
  echo_test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo hello
        run: echo "Hallo Tom 🚀 – dieser Workflow läuft alle 5 Minuten! Dies ist der kürzestet Intervall, der auf Github möglich ist!"
